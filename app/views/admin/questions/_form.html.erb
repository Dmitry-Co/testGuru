
<%= form_with model: [:admin, @test, @question], local: true do |form| %>
  <%= render 'shared/errors', resource: @question %>

  <p>
    <%= form.label t('.edit_body')  %><br>
    <%= form.text_area :body, id: :question_body, rows: 7, cols: 60 %>
  </p>

  <p><%= form.submit t('.save_question') %></p>

<% end %>


