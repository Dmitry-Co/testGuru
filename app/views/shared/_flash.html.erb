<% flash.each do |key, message| %>
  <% flash_class = key == 'notice' ? 'flash notice alert alert-success' : 'flash alert alert-danger' %>
  <%= content_tag :div, message, class: flash_class %>
 <% end %>
 